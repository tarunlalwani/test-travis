sudo: required # is required to use docker service in travis

language: node_js

node_js:
  - 'node'

services:
  - docker

before_install: 
  - which sh
  - which bash
  - echo "Testing this"
  
install: true
before_script: true
script: true
 
deploy:
  provider: script
  script: echo "Tarun lalwani"
  skip_cleanup: true
